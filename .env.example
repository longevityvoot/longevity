DATABASE_URL="postgresql://user:pass@localhost:5432/longevity?schema=public"

# NextAuth v5 — generate with: openssl rand -base64 32
AUTH_SECRET=
AUTH_URL=http://localhost:3000
# Legacy aliases (still read by some integrations)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (optional — login form falls back to credentials-only if unset)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
